const statusreport = document.getElementById("status-report");

statusreport.innerHTML += `<small class='text-muted'>under maintainance</small>`;

$(window).on('load', function() {
        $('#salutationModal').modal('show');
});